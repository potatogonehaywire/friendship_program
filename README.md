This is a program that pairs users based off of survey data. 
Their responses are converted into dataFrames, and similar users are ranked using a point system. 
Every user and their points are run through the Gale-Shapley alogrithm, which finds the ideal pair for each person.
The paired users are exported as one excel file.
